<div id="navbar-desktop" class="navbar-gfr">
    <div class="nav-main">
        <div class="navbar-left">
            <a href="/dashboard">
                <img src="https://www.depitskeuken.nl/wp-content/uploads/2018/10/cropped-pitslogovert-192x192.png" alt="logo">
            </a>
        </div>
        <div class="navbar-center">
            <div class="center-interior">
                <% if current_user.admin == true%>
                <div>
                    <% if @kitchen.kitchen_status %>
                    <%= button_to "Kitchen is now Open", kitchens_update_path, method: :patch, class: "btn btn-success" %>
                    <% else %>
                    <%= button_to "Kitchen is now Closed", kitchens_update_path, method: :patch, class: "btn btn-danger" %>
                    <% end %>
                </div>
                <% else %>
                <span class="navbar-welcome">Welcome,
                    <%= current_user.first_name %>!</span>
                <% end %>
            </div>
        </div>
        <div class="navbar-right">
            <% if current_user.admin == true%>
            <%= link_to "Dashboard", dashboard_path, class: "btn btn-dark" %>
            <% end %>
            <% if current_user.admin == false %>
            <% if Order.where(user: current_user, status: "in progress").first_or_create %>
            <%= link_to "My Cart", checkout_order_path(Order.where(user: current_user, status: "in progress").first_or_create), class: "btn btn-primary" %>
            <% end %>
            <%= link_to "My Orders", show_current_orders_path, class: "btn btn-success" %>
            <% end %>
            <% if current_user == nil%> -->
            <a class="btn btn-primary" href='/users/sign_in'>Login</a>
            <a class="btn btn-primary" href='/users/sign_up'>New User</a>
            <% else %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
            <% end %>
        </div>
    </div>
    <div class="nav-menu">
        <div class="item item1"><a class="menu-link btn btn-dark" href="#d-cat-daily">Specials</a></div>
        <div class="item item2"><a class="menu-link btn btn-dark" href="#d-cat-combi">Deals</a></div>
        <div class="item item3"><a class="menu-link btn btn-dark" href="#d-cat-losse">Mains</a></div>
        <div class="item item4"><a class="menu-link btn btn-dark" href="#d-cat-koude">Drinks</a></div>
        <div class="item item5"><a class="menu-link btn btn-dark" href="#d-cat-extras">Extras</a></div>
        iv class="item item5"><a class="menu-link btn btn-dark" href="#d-cat-desserts">Desserts</a></div>
    </div>
</div>
<div id="navbar-mobile" class="navbar-gfr">
    <div class="nav-main">
        <div class="navbar-left">
            <a href="/dashboard">
                <img src="https://www.depitskeuken.nl/wp-content/uploads/2018/10/cropped-pitslogovert-192x192.png" alt="logo">
            </a>
        </div>
        <div class="navbar-center">
            <div class="center-interior">
                <% if current_user.admin == true%>
                <div>
                    <% if @kitchen.kitchen_status %>
                    <%= button_to "Kitchen is now Open", kitchens_update_path, method: :patch, class: "btn btn-success" %>
                    <% else %>
                    <%= button_to "Kitchen is now Closed", kitchens_update_path, method: :patch, class: "btn btn-danger" %>
                    <% end %>
                </div>
                <% else %>
                <span class="navbar-welcome">Hey there,
                    <%= current_user.first_name %>!</span>
                <% end %>
            </div>
        </div>
        <div class="navbar-right">
            <div id="mainListDiv" class="main_list">
                <div class="collapse navbar-collapse show" id="navbar-list-4" style="margin-top: -0.5rem;">
                    <div class="navbar-nav">
                        <a class="nav-link" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-bars" style="font-size: 8vh; padding: 0; margin: 0;"></i>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <% if current_user.admin == true%>
                            <%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %>
                            <% end %>
                            <% if current_user.admin == false %>
                            <% if Order.where(user: current_user, status: "in progress").first_or_create %>
                            <%= link_to "🛒 My Cart", checkout_order_path(Order.where(user: current_user, status: "in progress").first_or_create), class: "dropdown-item" %>
                            <% end %>
                            <%= link_to "🍟 My Orders", show_current_orders_path, class: "dropdown-item" %>
                            <% end %>
                            <hr>
                            <%= link_to "✌🏻 Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="nav-menu">
        <main class="grid-item main">
            <div class="items">
                <div class="item item1"><a class="menu-link btn btn-dark" href="#cat-daily">Specials</a></div>
                <div class="item item2"><a class="menu-link btn btn-dark" href="#cat-combi">Deals</a></div>
                <div class="item item3"><a class="menu-link btn btn-dark" href="#cat-losse">Mains</a></div>
                <div class="item item4"><a class="menu-link btn btn-dark" href="#cat-koude">Drinks</a></div>
                <div class="item item5"><a class="menu-link btn btn-dark" href="#cat-extras">Extras</a></div>
                iv class="item item5"><a class="menu-link btn btn-dark" href="#cat-desserts">Desserts</a></div>
            </div>
        </main>
    </div>
</div>
