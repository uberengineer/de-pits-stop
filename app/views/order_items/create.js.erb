window.Swal.fire({
  icon: 'success',
  html: 'are in your cart',
  title: '<%= @order_item.quantity %> x <%= @order_item.menu_item.name %>',
  padding: '0.5em',
  text: 'are in your cart',
  confirmButtonText:'<%= link_to raw('<i class="fas fa-shopping-cart"></i> Go to cart'), checkout_order_path(@order_item.order), :class=>"buttontest" %>',
  footer: '<%= link_to 'add more items', menu_items_path %>'
})
